pipeline {
  agent { label 'master' }

  tools {
    maven 'Maven'
    jdk 'localJDK'
  }

  stages {
    stage('Build') {
      steps {
        sh 'mvn clean package'
        sh "docker build . -t tomcatwebapp:${env.BUILD_ID}"
        sh 'whoami'
      }
    }
  }
}
